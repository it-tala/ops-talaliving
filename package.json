{
  "name": "manufaktur-os",
  "version": "0.1.0",
  "private": true,
  "description": "Sistem manajemen internal manufaktur furnitur",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "//verify": "What to run before pushing. `build` is next build and it is NOT what ships: opennextjs-cloudflare build is, and it refuses things next build accepts — an edge-runtime route, for one, which cost a deploy on 2026-09-21. This is every step of the `app` CI job, in its order; cf:build runs next build itself, so `build` is not repeated. The database job is supabase/local/rebuild.sh + smoke.sh + check-view-contracts.mjs, which need a cluster.",
    "verify": "npx tsc --noEmit && npm run lint && node scripts/check-live-routes.mjs && node scripts/check-permissions.mjs && node scripts/check-api-parity.mjs && node scripts/check-api-schemas.mjs && npm run cf:build",
    "verify:db": "supabase/local/rebuild.sh && supabase/local/smoke.sh && node scripts/check-view-contracts.mjs && node scripts/check-schedule-rules.mjs && node scripts/check-john-lau.mjs && node scripts/sop/check-knowledge.mjs procurement && node scripts/sop/check-knowledge.mjs hr && supabase/import/test.sh",
    "start": "next start",
    "lint": "next lint",
    "cf:build": "opennextjs-cloudflare build",
    "cf:preview": "opennextjs-cloudflare preview",
    "cf:deploy": "opennextjs-cloudflare deploy"
  },
  "dependencies": {
    "@supabase/ssr": "^0.5.2",
    "@supabase/supabase-js": "^2.116.0",
    "clsx": "2.1.1",
    "lucide-react": "0.454.0",
    "next": "15.5.25",
    "qrcode-generator": "^2.0.4",
    "react": "19.3.0",
    "react-dom": "19.3.0",
    "recharts": "3.10.1",
    "swr": "^2.4.2",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@opennextjs/cloudflare": "^1.20.6",
    "@types/node": "20.16.11",
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "autoprefixer": "10.4.20",
    "eslint": "^8.57.1",
    "eslint-config-next": "^15.5.25",
    "jsqr": "^1.4.0",
    "postcss": "8.4.47",
    "tailwindcss": "3.4.13",
    "typescript": "5.6.3",
    "wrangler": "^4.133.0"
  }
}
